<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.familyaccount.mapper.RecordMapper">

    <!-- 多条件分页查询记录（关联类别表获取名称和图标） -->
    <select id="selectRecordPage" resultType="java.util.Map">
        SELECT
            r.id,
            r.type,
            r.category_id AS categoryId,
            r.amount,
            r.family_member AS familyMember,
            r.record_date AS recordDate,
            r.note,
            r.created_at AS createdAt,
            r.user_id AS userId,
            c.name AS categoryName,
            c.icon AS categoryIcon
        FROM record r
        LEFT JOIN category c ON r.category_id = c.id
        <where>
            <if test="familyId != null">
                AND r.family_id = #{familyId}
            </if>
            <if test="type != null and type != ''">
                AND r.type = #{type}
            </if>
            <if test="categoryId != null">
                AND r.category_id = #{categoryId}
            </if>
            <if test="familyMember != null and familyMember != ''">
                AND r.family_member = #{familyMember}
            </if>
            <if test="startDate != null and startDate != ''">
                AND r.record_date &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND r.record_date &lt;= #{endDate}
            </if>
            <if test="keyword != null and keyword != ''">
                AND r.note LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="userId != null">
                AND r.user_id = #{userId}
            </if>
        </where>
        ORDER BY r.record_date DESC, r.created_at DESC
    </select>

</mapper>
